# QA Reports

Runtime QA/regression scripts write JSON artifacts here.

Typical outputs:

- `route-regression-<timestamp>.json` from `scripts/qa/run-route-regression.sh`
- `route-regression-<timestamp>.json` from `scripts/qa/run-route-smoke.sh`

This directory is used for CI artifact publishing (`.github/workflows/ci-nightly.yml`).
