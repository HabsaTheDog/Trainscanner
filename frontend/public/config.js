// Frontend runtime map config.
// Set PROTOMAPS_API_KEY to use Protomaps vector styles with MapLibre.
// Optional override: MAP_STYLE_URL (must be a valid MapLibre style JSON URL).
// Optional override: SATELLITE_MAP_STYLE_URL (MapLibre style JSON for satellite mode).
window.PROTOMAPS_API_KEY = window.PROTOMAPS_API_KEY || '';
window.MAP_STYLE_URL = window.MAP_STYLE_URL || '';
window.SATELLITE_MAP_STYLE_URL = window.SATELLITE_MAP_STYLE_URL || '';
