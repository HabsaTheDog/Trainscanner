{
  "name": "motis-gtfs-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "MVP orchestrator for GTFS profile switching and MOTIS routing",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/switcher.js && node --check src/motis.js"
  },
  "engines": {
    "node": ">=20"
  }
}
